import { env, isDeno, runtime } from "std-env";

//#region src/core/runtime.ts
/**
* Runtime abstraction — portable environment access with no context facet.
*
* Reading configuration differs per host — `Deno.env.get` on Deno, `process.env`
* on Node/Bun, a per-request bindings object on Cloudflare Workers.
* {@link getEnv} normalizes that to a single importable function:
*
* ```ts
* import { getEnv } from '@supabase/middleware'
*
* getEnv('SUPABASE_DB_URL') // string | undefined, resolved per host
* ```
*
* Host detection is delegated to [`std-env`](https://github.com/unjs/std-env)
* (which tracks the WinterCG Runtime Keys proposal) — {@link runtimeName}
* re-exports its detected name. On Workers, env bindings are not ambient: they
* arrive per request as the second `fetch` argument. The entry call captures
* that object module-scoped (see {@link seedContext}), and {@link getEnv} reads
* it first before falling back to the host's global env. The one consequence:
* on Workers, `getEnv` returns `undefined` at module top level, before the
* first request has been seen.
*
* There is **no wrapper step**. A composed stack is used directly as the
* runtime's `fetch` handler (`export default { fetch: withFoo(config, handler) }`).
* When the host invokes the outermost handler, {@link defineMiddleware} detects
* that the second argument is not an upstream context (via {@link isContext})
* and seeds a fresh context itself — so a host-supplied `env` /
* `ServeHandlerInfo` is never merged into `ctx`. The seeded context is empty
* except for a non-enumerable-to-`Object.keys` symbol marker: `ctx` carries
* middleware contributions and nothing else.
*
* The request body is made re-readable on `req` itself (see {@link bufferRequest})
* rather than via a `ctx` key, so the body stays a property of the request.
*
* @packageDocumentation
*/
/**
* The host's detected runtime name, re-exported from `std-env` (WinterCG
* Runtime Keys: `'node' | 'deno' | 'bun' | 'workerd' | 'edge-light' | …`, or
* `''` when unknown). Detected once at module load.
*/
const runtimeName = runtime;
/**
* Platform-provided bindings, captured module-scoped from the entry call. On
* Cloudflare Workers this is the per-request `env` object (same object for
* every request in an isolate); on Deno it is the `ServeHandlerInfo` (whose
* keys never look like env vars, so lookups simply fall through).
*/
let platformEnv;
/**
* Resolve an environment value the same way regardless of host, so middleware
* never branch on `Deno` vs `process` vs a Workers bindings object.
*
* Resolution order:
* 1. the platform object captured at the entry call (Workers bindings),
* 2. `process.env` where a `process` global exists (Node, Bun, Deno 2,
*    Workers with `nodejs_compat`), via `std-env`,
* 3. `Deno.env.get` on Deno hosts without a populated `process.env`.
*
* On Workers, bindings are per-request, so this returns `undefined` at module
* top level until the first request has been handled.
*/
function getEnv(key) {
	if (platformEnv) {
		const bound = platformEnv[key];
		if (typeof bound === "string") return bound;
	}
	const fromStd = env[key];
	if (fromStd !== void 0) return fromStd;
	if (isDeno) return globalThis.Deno?.env?.get(key);
}
/**
* Marks a context object seeded by this framework, so an entry call can tell an
* upstream context apart from a host-supplied platform argument (a Workers
* `env`, a Deno `ServeHandlerInfo`) occupying the same positional slot.
* `Symbol.for` (the global registry) so contexts cross module instances — e.g.
* the CJS and ESM builds loaded side by side — and still recognize each other.
*/
const CONTEXT_MARK = Symbol.for("@supabase/middleware:context");
/**
* Seed a fresh base context for an entry call, capturing the host's second
* `fetch` argument (a Workers `env`, a Deno `ServeHandlerInfo`, …) as the
* module-scoped platform env for {@link getEnv}. The platform value itself is
* never merged into `ctx`.
*
* Public so a host embedding the engine (e.g. `@supabase/server`) can mint a
* valid upstream context and spread its own keys onto it.
*/
function seedContext(platformArg) {
	if (platformArg && typeof platformArg === "object") platformEnv = platformArg;
	return { [CONTEXT_MARK]: true };
}
/**
* Distinguish an upstream context (passed by a parent middleware) from a
* host-supplied platform argument (an `env` / connection-info object the runtime
* puts in the same positional slot). The symbol marker set by {@link seedContext}
* flows by reference through every `{ ...upstream }` merge (spread copies
* enumerable own symbols), so checking for it is reliable across the stack.
*
* Public so a host embedding the engine (e.g. `@supabase/server`) can make the
* same distinction before calling {@link seedContext}: reseeding with an
* upstream context would stash it as the platform env, clobbering the real
* bindings captured earlier.
*/
function isContext(value) {
	return !!value && typeof value === "object" && CONTEXT_MARK in value;
}
/** Body-reading `Request` methods the buffered proxy re-implements from a single cached read. */
const BUFFERED_METHODS = new Set([
	"arrayBuffer",
	"bytes",
	"blob",
	"text",
	"json",
	"formData"
]);
/**
* Wrap a `Request` so its body can be read more than once.
*
* A Fetch `Request` body is a single-use stream — the first reader of
* `req.text()` / `req.json()` / … locks out every later one. This returns a
* proxy that reads the underlying body **at most once** and caches the bytes, so
* a body-verifying middleware (e.g. a webhook signature check) and the handler can each read it, in
* any form:
*
* - `arrayBuffer` / `bytes` / `blob` / `text` / `json` / **`formData`** all read
*   from the one cached read (`formData` is parsed from the cached bytes using
*   the request's `content-type`).
* - **`clone()`** returns another handle over the same cache — reading either
*   yields the same body; `headers` / `url` / `method` / `signal` / … forward to
*   the real request, and `proxy instanceof Request` stays true.
*
* The proxy is created once at the entry call and flows down the stack, so the
* cache is shared across every layer.
*
* **One deliberate limit:** reading the raw `req.body` *stream* (e.g. handing the
* request to `fetch()` to forward it) bypasses the cache. By design this is a
* *buffering* model, not a streaming one — to forward the body, reconstruct it,
* e.g. `new Request(req.url, { method: req.method, headers: req.headers, body:
* await req.arrayBuffer() })`.
*/
function bufferRequest(req) {
	let buffer;
	const arrayBuffer = () => buffer ??= req.arrayBuffer();
	const text = async () => new TextDecoder().decode(await arrayBuffer());
	const cached = {
		arrayBuffer,
		text,
		json: async () => JSON.parse(await text()),
		bytes: async () => new Uint8Array(await arrayBuffer()),
		blob: async () => new Blob([await arrayBuffer()]),
		formData: async () => new Response(await arrayBuffer(), { headers: req.headers }).formData()
	};
	const handler = { get(target, prop) {
		if (typeof prop === "string" && BUFFERED_METHODS.has(prop)) return cached[prop];
		if (prop === "clone") return () => new Proxy(target, handler);
		const value = Reflect.get(target, prop, target);
		return typeof value === "function" ? value.bind(target) : value;
	} };
	return new Proxy(req, handler);
}

//#endregion
//#region src/core/define-middleware.ts
/**
* Warn (once per process) that a host passed a third `fetch` argument — the
* Workers `ExecutionContext` (`waitUntil` / `passThroughOnException`) — which is
* not honored. The request still proceeds; only the execution context is ignored.
*/
let warnedThirdArg = false;
function warnUnhonoredThirdArg() {
	if (warnedThirdArg) return;
	warnedThirdArg = true;
	console.warn("middleware: a third fetch argument (the Workers ExecutionContext / waitUntil) was supplied but is not honored; it will be ignored. Supported entry signatures are (request) and (request, env).");
}
/**
* Defines a middleware.
*
* A middleware runs against an inbound `Request` and the upstream context. It
* either short-circuits by returning a `Response`, or contributes a typed value
* at `ctx[key]` by returning a single-key object `{ [key]: contribution }` — the
* framework picks `result[key]`, merges it into the context, and calls the inner
* handler. Extra keys on the returned object are ignored at runtime. Note they
* are **not** caught at compile time: `run`'s return is contextually typed
* against a `Response | { [key]: … }` union via a generic mapped type, a position
* where TypeScript suppresses excess-property checks — so a stray sibling key
* slips past the types (harmlessly). The runtime {@link contributionOf} guard is
* the backstop, and it throws only when the key is *missing* entirely (e.g. a
* computed/typo'd key the types couldn't see). To opt into the excess check on a
* given middleware, annotate `run`'s inner return type explicitly.
*
* `run` is **request-side by default** (the common case): it runs *before* the
* handler and never observes the handler's `Response`. Response-shaped concerns
* (CORS, envelopes) normally belong in the handler or a `.then()` on the stack.
*
* **Response seam (escape hatch).** When a middleware genuinely needs to see the
* way out — stamp headers, time the request, run `finally` cleanup — write `run`
* as an `async function*` instead of `async`. `yield` is the seam: code before it
* is the request phase; you `yield` the contribution and the `yield` expression
* resolves to the downstream `Response` for the response phase. `yield` always
* means "run downstream and hand me the response" — short-circuit with a plain
* `return new Response(...)`, exactly as the request-side path does, and yield at
* most once. This is the one place a middleware observes the handler's response,
* and writing `function*` is the visible, opt-in signal.
*
* `withFoo(config, handler)` produces a single `(req, ctx) => Response` function.
* Middleware nest directly, and the **outermost is used as the runtime's `fetch`
* handler with no wrapper** — `export default { fetch: withFoo(config, handler) }`.
* When the host invokes it, the second argument is a platform value (a Workers
* `env`, a Deno `ServeHandlerInfo`), not an upstream context; the wrapper detects
* this via {@link isContext} and seeds a fresh context instead of merging it, so
* platform arguments never leak into `ctx` — they are only captured as the
* module-scoped platform env behind the importable `getEnv`.
*
* Typing:
*
* - **Prerequisite-free middleware can be the `fetch` export on their own.**
*   Their produced handler has an optional `ctx`, so it satisfies a bare
*   `(req) => Response` export and self-seeds a fresh context.
* - **Middleware with `In` prerequisites require `ctx`.** They can only be nested
*   inside a wrapper that supplies those keys — never the bare `fetch` export —
*   which keeps the prerequisite from being a type-lie at the top level. The
*   supplying wrapper need not be the immediately enclosing one, and no
*   annotation is needed: an unmet prerequisite is republished by each layer that
*   doesn't contribute it, travelling outward until one does. If none does, the
*   stack keeps a required `ctx` and fails wherever it is checked against
*   {@link FetchHandler} — but an untyped `export default { fetch: … }` is no
*   such check, so annotate the outermost call to catch it at build time.
* - **Accumulation needs no annotation.** The innermost handler sees every
*   upstream key ambiently at any nesting depth. An unannotated outermost call
*   has no contextual return type, so `Base` resolves to its constraint — the
*   empty upstream, which is exactly what a `satisfies FetchHandler` would seed
*   — and the cascade proceeds inward from there.
* - **Collision detection does.** Composing where the upstream already has the
*   key resolves the handler parameter to a {@link Conflict} sentinel (see
*   {@link NoConflict}) and the stack fails to typecheck — but only under
*   `satisfies FetchHandler` on the outermost call. The produced handler type
*   records the upstream a stack *requires*, never the keys it *contributes*, so
*   an unannotated enclosing call has nothing to check its own key against and a
*   duplicate compiles silently. One annotation covers any depth.
*   {@link pipeline} has no such gap — it validates from its entries array — so
*   a stack that cannot carry the annotation is better written flat.
*
* @typeParam Key - The literal-string key contributed to ctx.
* @typeParam Config - Configuration object the middleware accepts.
* @typeParam In - Upstream prerequisites besides {@link BaseContext}. Defaults to none.
* @typeParam Contribution - Shape of the value placed at `ctx[Key]`.
*
* @example
* ```ts
* import { defineMiddleware } from '@supabase/middleware'
*
* export const withFeatureFlag = defineMiddleware<
*   'featureFlag',
*   { name: string; evaluate: (req: Request) => boolean },
*   {},
*   { name: string; enabled: true }
* >({
*   key: 'featureFlag',
*   run: (config) => async (req) => {
*     if (!config.evaluate(req)) {
*       return Response.json({ error: 'feature_disabled' }, { status: 404 })
*     }
*     return { featureFlag: { name: config.name, enabled: true } }
*   },
* })
* ```
*/
function defineMiddleware(spec) {
	const callable = (...args) => {
		const lastArg = args[args.length - 1];
		if (args.length === 0 || typeof lastArg !== "function") {
			const config = args.length > 0 ? args[0] : void 0;
			const wrap = (handler) => callable(config, handler);
			return wrap;
		}
		const config = args.length >= 2 ? args[0] : void 0;
		const handler = lastArg;
		const inner = spec.run(config);
		return async (req, maybeCtx, ...rest) => {
			let workingReq = req;
			let upstream;
			if (isContext(maybeCtx)) upstream = maybeCtx;
			else {
				if (rest.length > 0) warnUnhonoredThirdArg();
				workingReq = req.body ? bufferRequest(req) : req;
				upstream = seedContext(maybeCtx);
			}
			const runInner = handler;
			const callDownstream = (contribution) => runInner(workingReq, {
				...upstream,
				[spec.key]: contribution
			});
			const produced = inner(workingReq, upstream);
			if (!isAsyncGenerator(produced)) {
				const result = await produced;
				if (result instanceof Response) return result;
				return callDownstream(contributionOf(result, spec.key));
			}
			const gen = produced;
			const first = await gen.next();
			if (first.value instanceof Response) {
				if (!first.done) await gen.return(void 0);
				return first.value;
			}
			const contribution = contributionOf(first.value, spec.key);
			if (first.done) return callDownstream(contribution);
			let response;
			try {
				response = await callDownstream(contribution);
			} catch (err) {
				const recovered = await gen.throw(err);
				if (recovered.value instanceof Response) return recovered.value;
				throw err;
			}
			const resumed = await gen.next(response);
			if (!resumed.done) await gen.return(void 0);
			return resumed.value instanceof Response ? resumed.value : response;
		};
	};
	return callable;
}
/**
* Narrow a `run` result to the async-generator (onion) form. A plain `async`
* body returns a `Promise`; an `async function*` body returns an async
* generator, which is what carries the `yield` seam. The two are disjoint, so a
* single `Symbol.asyncIterator` probe picks the path with no author ceremony.
*/
function isAsyncGenerator(value) {
	return value != null && typeof value[Symbol.asyncIterator] === "function" && typeof value.next === "function";
}
/**
* Pull the contribution off a fall-through result. Defensive: catches authoring
* bugs the type system can't, e.g. a typo in the key that slipped past
* excess-property checks, or a generator that produced nothing.
*/
function contributionOf(result, key) {
	if (result === null || typeof result !== "object" || !(key in result)) throw new Error(`defineMiddleware '${key}': run() must return or yield an object carrying the key '${key}'`);
	return result[key];
}

//#endregion
export { seedContext as a, runtimeName as i, getEnv as n, isContext as r, defineMiddleware as t };