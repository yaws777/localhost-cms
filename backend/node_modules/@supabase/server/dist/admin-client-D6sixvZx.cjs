const require_errors = require("./errors-ClZWGicV.cjs");
const require_create_admin_client = require("./create-admin-client-SwEa-fjf.cjs");
const require_read_upstream_auth = require("./read-upstream-auth-yQlJO74r.cjs");
let _supabase_middleware = require("@supabase/middleware");
//#region src/core/lazy-client.ts
/**
* Wraps a client factory in a Proxy that defers construction to the first
* interaction with the client — a property read or write, an `in` check,
* `instanceof`, or key enumeration. Construction happens at most once: every
* trap delegates to the same memoized instance. A factory throw surfaces at
* the interaction that triggered it, and the factory runs again on the next
* interaction.
*
* Function-valued properties are bound to the real instance, so class
* internals (private fields, prototype getters) always see the instance
* itself rather than the proxy.
*
* Inspecting an unconstructed proxy (`console.log`, test diff printers)
* enumerates its keys and therefore triggers construction — in a
* misconfigured environment, that inspection throws.
*/
function lazyClient(build) {
	let client;
	const instance = () => client ??= build();
	return new Proxy({}, {
		get(_target, prop) {
			const c = instance();
			const value = Reflect.get(c, prop);
			return typeof value === "function" ? value.bind(c) : value;
		},
		set: (_target, prop, value) => Reflect.set(instance(), prop, value),
		has: (_target, prop) => Reflect.has(instance(), prop),
		getPrototypeOf: () => Reflect.getPrototypeOf(instance()),
		ownKeys: () => Reflect.ownKeys(instance()),
		getOwnPropertyDescriptor: (_target, prop) => Reflect.getOwnPropertyDescriptor(instance(), prop)
	});
}
//#endregion
//#region src/middleware/admin-client/index.ts
const base = (0, _supabase_middleware.defineMiddleware)({
	key: "supabaseAdmin",
	run: (config) => async (_req, ctx) => {
		const upstream = require_read_upstream_auth.readUpstreamAuth(ctx);
		const keyName = upstream.authMode === "secret" ? upstream.authKeyName : void 0;
		return { supabaseAdmin: lazyClient(() => {
			try {
				return require_create_admin_client.createAdminClient({
					auth: { keyName },
					env: config?.env,
					supabaseOptions: config?.supabaseOptions
				});
			} catch (e) {
				throw e instanceof require_errors.EnvError ? e : require_errors.Errors[require_errors.CreateSupabaseClientError]({ cause: e });
			}
		}) };
	}
});
/**
* **Alpha.** Contributes `ctx.supabaseAdmin` — an admin Supabase client that
* bypasses Row-Level Security, authenticated with a secret key. This is the
* same middleware `withSupabase` composes internally to build its context.
*
* The client is constructed on the first property access of
* `ctx.supabaseAdmin` and memoized for the request. A handler that never
* accesses it requires no secret key.
*
* @throws {@link index.EnvError} When `SUPABASE_URL` or the secret key is
* missing — thrown at the first `ctx.supabaseAdmin` property access, inside
* the handler or downstream middleware that performs it.
*
* @example Standalone pipeline
* ```ts
* import { pipeline } from '@supabase/middleware'
* import { withSupabaseAdminClient } from '@supabase/server/middleware/admin-client'
*
* export default {
*   fetch: pipeline([withSupabaseAdminClient()], async (req, ctx) => {
*     await ctx.supabaseAdmin.from('audit_log').insert({ action: 'ping' })
*     return Response.json({ ok: true })
*   }),
* }
* ```
*
* The composable middleware surface tracks `@supabase/middleware` 0.x — entry
* shapes, context keys, and config options may change between 0.x releases.
*
* @alpha
* @category Middleware
*/
function withSupabaseAdminClient(config) {
	return base(config);
}
//#endregion
Object.defineProperty(exports, "withSupabaseAdminClient", {
	enumerable: true,
	get: function() {
		return withSupabaseAdminClient;
	}
});
