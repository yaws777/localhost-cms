//#region src/errors.ts
/**
* Package identifier stamped on every error this library produces.
*
* Present as `source` on each error instance, as the `source` field of the JSON
* body {@link withSupabase} returns, and as a `[@supabase/server]` prefix on
* every `message` — so an error is traceable back here from a log line, a
* response body, or a response header alone.
*
* @category Errors
*/
const ErrorSource = "@supabase/server";
/** Prefix prepended to every error message. @internal */
const MessagePrefix = `[${ErrorSource}] `;
/**
* Response header {@link withSupabase} sets on every error response, carrying
* the error {@link SupabaseServerError.code}.
*
* @category Errors
*/
const ErrorCodeHeader = "x-supabase-server-error";
/** @internal */
const DocsBase = "https://github.com/supabase/server/blob/main/docs/error-handling.md";
/**
* Builds the documentation URL for an error code. Anchors match the
* `### CODE` headings in `docs/error-handling.md`.
*
* @internal
*/
function docsFor(code) {
	return `${DocsBase}#${code.toLowerCase()}`;
}
/** Renders a list of names as `"a", "b"` for interpolation into messages. @internal */
function quoteList(items) {
	return items.map((item) => `"${item}"`).join(", ");
}
/**
* Base class for every error `@supabase/server` produces.
*
* Catch this to handle anything originating from this library, regardless of
* whether it's an {@link EnvError} or an {@link AuthError}:
*
* @example Catching any library error
* ```ts
* import { SupabaseServerError } from '@supabase/server'
*
* try {
*   const supabase = createAdminClient()
* } catch (e) {
*   if (e instanceof SupabaseServerError) {
*     console.error(e.code, e.message, e.hint, e.docs)
*     return Response.json(e.toJSON(), { status: e.status })
*   }
*   throw e
* }
* ```
*
* @category Errors
*/
var SupabaseServerError = class extends Error {
	constructor(message, code, options) {
		super(message.startsWith(MessagePrefix) ? message : MessagePrefix + message, options && "cause" in options ? { cause: options.cause } : void 0);
		this.source = ErrorSource;
		this.name = "SupabaseServerError";
		this.code = code;
		this.docs = options?.docs ?? docsFor(code);
		if (options?.hint) this.hint = options.hint;
		if (options?.details) this.details = options.details;
	}
	/**
	* Returns the wire format — also used implicitly by `JSON.stringify`, so
	* logging the error yields the full diagnostics rather than `{}`.
	*/
	toJSON() {
		return {
			source: this.source,
			code: this.code,
			message: this.message,
			...this.hint ? { hint: this.hint } : {},
			docs: this.docs,
			...this.details ? { details: this.details } : {}
		};
	}
};
/**
* Thrown when a required environment variable is missing or malformed.
*
* Always has `status: 500` — environment errors are server-side configuration issues.
*
* @example Catching an EnvError
* ```ts
* import { EnvError } from '@supabase/server'
*
* try {
*   const client = createAdminClient()
* } catch (e) {
*   if (e instanceof EnvError) {
*     console.error(`[${e.code}] ${e.message}\n${e.hint}`)
*     // → "[MISSING_SUPABASE_URL] [@supabase/server] SUPABASE_URL is required but not set."
*     //   "Set SUPABASE_URL to your project URL (https://<project-ref>.supabase.co), …"
*   }
* }
* ```
*
* @category Errors
*/
var EnvError = class extends SupabaseServerError {
	/**
	* @param message - Human-readable description. Prefixed with `[@supabase/server]`.
	* @param code - Machine-readable code. @see {@link EnvGenericError}, {@link MissingSupabaseURLError},
	*   {@link MissingPublishableKeyError}, {@link MissingDefaultPublishableKeyError},
	*   {@link MissingSecretKeyError}, {@link MissingDefaultSecretKeyError}
	* @param options - Optional `hint`, `details`, `docs`, and `cause`.
	*/
	constructor(message, code = EnvGenericError, options) {
		super(message, code, options);
		this.status = 500;
		this.name = "EnvError";
	}
};
/**
* Generic environment error code.
* @category Errors
*/
const EnvGenericError = "ENV_ERROR";
/**
* `SUPABASE_URL` is not set.
* @category Errors
*/
const MissingSupabaseURLError = "MISSING_SUPABASE_URL";
/**
* Named publishable key not found in `SUPABASE_PUBLISHABLE_KEYS`.
* @category Errors
*/
const MissingPublishableKeyError = "MISSING_PUBLISHABLE_KEY";
/**
* No default publishable key found.
* @category Errors
*/
const MissingDefaultPublishableKeyError = "MISSING_DEFAULT_PUBLISHABLE_KEY";
/**
* Named secret key not found in `SUPABASE_SECRET_KEYS`.
* @category Errors
*/
const MissingSecretKeyError = "MISSING_SECRET_KEY";
/**
* No default secret key found.
* @category Errors
*/
const MissingDefaultSecretKeyError = "MISSING_DEFAULT_SECRET_KEY";
/**
* `withOAuthProtectedResource` has no `resourceServer` and cannot derive one.
* @category Errors
*/
const MissingResourceServerError = "MISSING_RESOURCE_SERVER";
/**
* `withOAuthProtectedResource` has no `authorizationServer` and cannot derive one.
* @category Errors
*/
const MissingAuthorizationServerError = "MISSING_AUTHORIZATION_SERVER";
/**
* No Postgres connection string is configured, so the `withPostgresClient` /
* `withPostgresAdminClient` middleware cannot connect.
*
* @category Errors
*/
const MissingConnectionStringError = "MISSING_CONNECTION_STRING";
/**
* Describes the configured key names for a hint without revealing key values.
* @internal
*/
function configuredNames(available) {
	if (!available || available.length === 0) return "None are configured.";
	return `Configured names: ${quoteList(available)}.`;
}
const EnvErrorMap = {
	[MissingSupabaseURLError]: () => new EnvError("SUPABASE_URL is required but not set.", MissingSupabaseURLError, { hint: "Set SUPABASE_URL to your project URL (https://<project-ref>.supabase.co), or pass `env.url`. A local Supabase CLI stack uses http://localhost:54321." }),
	[MissingSecretKeyError]: (name, availableKeyNames) => new EnvError(`No "${name}" secret key found. ${configuredNames(availableKeyNames)}`, MissingSecretKeyError, {
		hint: `Add a "${name}" entry to SUPABASE_SECRET_KEYS — a JSON object of name → key, e.g. {"${name}":"sb_secret_…"} — or pass \`env.secretKeys\`.`,
		details: {
			requestedKeyName: name,
			configuredKeyNames: availableKeyNames ?? []
		}
	}),
	[MissingDefaultSecretKeyError]: (availableKeyNames) => new EnvError(`No default secret key found. ${configuredNames(availableKeyNames)}`, MissingDefaultSecretKeyError, {
		hint: "Set SUPABASE_SECRET_KEY, or add a \"default\" entry to SUPABASE_SECRET_KEYS (a JSON object of name → key), or pass `env.secretKeys`.",
		details: { configuredKeyNames: availableKeyNames ?? [] }
	}),
	[MissingPublishableKeyError]: (name, availableKeyNames) => new EnvError(`No "${name}" publishable key found. ${configuredNames(availableKeyNames)}`, MissingPublishableKeyError, {
		hint: `Add a "${name}" entry to SUPABASE_PUBLISHABLE_KEYS — a JSON object of name → key, e.g. {"${name}":"sb_publishable_…"} — or pass \`env.publishableKeys\`.`,
		details: {
			requestedKeyName: name,
			configuredKeyNames: availableKeyNames ?? []
		}
	}),
	[MissingDefaultPublishableKeyError]: (availableKeyNames) => new EnvError(`No default publishable key found. ${configuredNames(availableKeyNames)}`, MissingDefaultPublishableKeyError, {
		hint: "Set SUPABASE_PUBLISHABLE_KEY, or add a \"default\" entry to SUPABASE_PUBLISHABLE_KEYS (a JSON object of name → key), or pass `env.publishableKeys`.",
		details: { configuredKeyNames: availableKeyNames ?? [] }
	}),
	[MissingResourceServerError]: () => new EnvError("resourceServer is required outside Supabase Edge Functions and could not be derived.", MissingResourceServerError, { hint: "Pass it to withOAuthProtectedResource(), e.g. { resourceServer: (req) => new URL(req.url).origin + '/api/mcp' }. On Edge Functions it is derived from the request instead." }),
	[MissingAuthorizationServerError]: () => new EnvError("authorizationServer is required outside Supabase Edge Functions and could not be derived.", MissingAuthorizationServerError, { hint: "Pass it to withOAuthProtectedResource() — use fromSupabaseUrl('https://<project-ref>.supabase.co') for Supabase Auth — or set SUPABASE_PUBLIC_URL or SUPABASE_URL." }),
	[MissingConnectionStringError]: (middleware) => new EnvError("A Postgres connection string is required, and none is configured.", MissingConnectionStringError, {
		hint: `Set SUPABASE_DB_URL, or pass \`connectionString\` to ${middleware}(). Supabase Edge Functions provide SUPABASE_DB_URL automatically; elsewhere, copy it from Project Settings → Database → Connection string.`,
		details: { middleware }
	})
};
/**
* Thrown when authentication or authorization fails.
*
* Carries an HTTP `status` code suitable for returning directly in a response
* (`401` when the request's credentials are at fault, `500` when the server is
* misconfigured and could not have authenticated anyone).
*
* @example Catching an AuthError
* ```ts
* import { createSupabaseContext } from '@supabase/server'
*
* const { data: ctx, error } = await createSupabaseContext(request, { auth: 'user' })
* if (error) {
*   // error is an AuthError — `toJSON()` includes source, code, message, hint, docs, details
*   return Response.json(error.toJSON(), { status: error.status })
* }
* ```
*
* @category Errors
*/
var AuthError = class extends SupabaseServerError {
	/**
	* @param message - Human-readable description. Prefixed with `[@supabase/server]`.
	* @param code - Machine-readable code. @see {@link AuthGenericError}, {@link MissingCredentialsError},
	*   {@link InvalidApiKeyError}, {@link InvalidJwtError}, {@link InvalidCredentialsError},
	*   {@link JwksNotConfiguredError}, {@link JwksFetchFailedError},
	*   {@link NoKeysConfiguredError}, {@link CreateSupabaseClientError}
	* @param status - HTTP status. Defaults to `401`.
	* @param options - Optional `hint`, `details`, `docs`, and `cause`.
	*/
	constructor(message, code = AuthGenericError, status = 401, options) {
		super(message, code, options);
		this.name = "AuthError";
		this.status = status;
	}
};
/**
* Generic authentication error code.
* @category Errors
*/
const AuthGenericError = "AUTH_ERROR";
/**
* The request carried no credentials at all — neither an `apikey` header nor
* an `Authorization: Bearer` token — and no accepted auth mode allows that.
*
* @category Errors
*/
const MissingCredentialsError = "MISSING_CREDENTIALS";
/**
* A credential *did* arrive, but not one any accepted auth mode can use — an
* `sb_*` API key in the `Authorization` header, or a header this library cannot
* read a bearer token out of (wrong scheme, wrong casing, bare value, empty
* token).
*
* @remarks Distinct from {@link MissingCredentialsError} on purpose. The two
* partition the "nothing authenticated" space exactly — nothing arrived vs.
* something arrived that could not be used — so the code stays true on its own
* when `errors: { detailed: false }` strips `hint` and `details`.
*
* @category Errors
*/
const UnusableCredentialError = "UNUSABLE_CREDENTIAL";
/**
* An `apikey` header was present but matched none of the keys configured for
* the accepted auth modes.
*
* @category Errors
*/
const InvalidApiKeyError = "INVALID_API_KEY";
/**
* A JWT was present in the `Authorization` header but failed verification.
* `details.jwt` and the message carry the specific reason (expired, bad
* signature, unknown `kid`, malformed, no `sub`).
*
* @category Errors
*/
const InvalidJwtError = "INVALID_JWT";
/**
* Generic credential failure. Retained as the fallback code for cases that
* don't match a more specific one.
*
* @remarks Prior to v1.5 this was the only code {@link withSupabase} ever
* returned for a failed request. Prefer matching on the specific codes —
* {@link MissingCredentialsError}, {@link InvalidApiKeyError},
* {@link InvalidJwtError} — which now cover nearly every real failure.
*
* @category Errors
*/
const InvalidCredentialsError = "INVALID_CREDENTIALS";
/**
* Auth mode `"user"` was requested and a JWT was supplied, but no JWKS is
* configured — so the token cannot be verified. Server misconfiguration
* (`status: 500`), not a bad request.
*
* @category Errors
*/
const JwksNotConfiguredError = "JWKS_NOT_CONFIGURED";
/**
* The remote JWKS endpoint could not be fetched or returned something
* unusable. Server-side / upstream failure (`status: 500`), not a bad request.
*
* @category Errors
*/
const JwksFetchFailedError = "JWKS_FETCH_FAILED";
/**
* An `"publishable"` or `"secret"` auth mode was requested but no keys of that
* kind are configured, so the mode could never match. Server
* misconfiguration (`status: 500`).
*
* @category Errors
*/
const NoKeysConfiguredError = "NO_KEYS_CONFIGURED";
/**
* Failed to create a Supabase client after auth succeeded.
* @category Errors
*/
const CreateSupabaseClientError = "CREATE_SUPABASE_CLIENT_ERROR";
/**
* The caller's verified `role` claim names a Postgres role the middleware
* will not assume — either one that would bypass RLS, or one it does not
* support yet. Returned by `withPostgresClient` instead of silently running
* the query as `anon`.
* @category Errors
*/
const UnsupportedRoleError = "UNSUPPORTED_ROLE";
/**
* Maps an auth mode to the header the caller must send for it to match.
* @internal
*/
function credentialForMode(mode) {
	if (mode === "user") return "Authorization: Bearer <jwt>";
	if (mode === "publishable" || mode.startsWith("publishable:")) return "apikey: <publishable key>";
	if (mode === "secret" || mode.startsWith("secret:")) return "apikey: <secret key>";
	return null;
}
/**
* Builds a "send one of these" hint from the attempted auth modes.
* @internal
*/
function sendOneOfHint(authModes) {
	const options = [...new Set(authModes.map((mode) => {
		const credential = credentialForMode(mode);
		return credential ? `${credential} (for auth mode "${mode}")` : null;
	}).filter((entry) => entry !== null))];
	if (options.length === 0) return void 0;
	return `${options.length > 1 ? "Send one of" : "Send"}: ${options.join("; ")}.`;
}
/** Human label for a key format, for use in hints. @internal */
const ApiKeyFormatLabel = {
	absent: "no apikey header",
	publishable: "a publishable key (sb_publishable_…)",
	secret: "a secret key (sb_secret_…)",
	"legacy-jwt": "a legacy JWT-style key (eyJ…)",
	unrecognized: "a value in an unrecognized format"
};
/**
* Explains an API key rejection, favouring the format-mismatch case — sending
* a secret key to a publishable-only endpoint (or vice versa) is a far more
* common mistake than a genuinely wrong key.
*
* @internal
*/
function apiKeyHint(context) {
	const { authModes, received } = context;
	const keyModes = authModes.filter((mode) => credentialForMode(mode)?.startsWith("apikey"));
	const acceptsPublishable = keyModes.some((mode) => mode.startsWith("publishable"));
	const acceptsSecret = keyModes.some((mode) => mode.startsWith("secret"));
	if (received.apikey === "legacy-jwt") return "The apikey looks like a legacy JWT-based key (anon / service_role). @supabase/server expects the newer API key format — sb_publishable_… or sb_secret_…. Find them under Project Settings → API Keys.";
	if (received.apikey === "secret" && acceptsPublishable && !acceptsSecret) return "You sent a secret key, but this endpoint only accepts publishable keys. Send a sb_publishable_… key, or add \"secret\" to `auth` if server-to-server calls should be allowed.";
	if (received.apikey === "publishable" && acceptsSecret && !acceptsPublishable) return "You sent a publishable key, but this endpoint only accepts secret keys. Send a sb_secret_… key, or add \"publishable\" to `auth` if client-facing calls should be allowed.";
	if (received.apikey === "unrecognized") return `The apikey header held ${ApiKeyFormatLabel.unrecognized} — Supabase API keys start with sb_publishable_ or sb_secret_. Copy the key from Project Settings → API Keys.`;
	const kinds = Object.entries(context.configuredKeyNames ?? {});
	const names = kinds.map(([kind, keyNames]) => `${kind}: ${keyNames.length ? quoteList(keyNames) : "none configured"}`).join("; ");
	const envVars = kinds.map(([kind]) => `SUPABASE_${kind.toUpperCase()}_KEY(S)`).join(" / ");
	return `The key was well-formed but matched no configured key${names ? ` (${names})` : ""}. Keys come from ${envVars || "the SUPABASE_*_KEY(S) variables"}, or the \`env\` option. Check you are pointing at the right Supabase project.`;
}
const AuthErrorMap = {
	[MissingCredentialsError]: (context) => new AuthError(`No credentials found on the request. This endpoint accepts auth mode(s): ${quoteList(context.authModes)}.`, MissingCredentialsError, 401, {
		hint: sendOneOfHint(context.authModes),
		details: {
			acceptedAuthModes: context.authModes,
			received: context.received
		}
	}),
	[UnusableCredentialError]: (context) => new AuthError(`The request carried a credential this endpoint cannot use: ${context.reason}.` + (context.authModes ? ` Accepted auth mode(s): ${quoteList(context.authModes)}.` : ""), UnusableCredentialError, 401, {
		hint: [context.hint, context.authModes && sendOneOfHint(context.authModes)].filter(Boolean).join(" "),
		details: {
			...context.authModes ? { acceptedAuthModes: context.authModes } : {},
			...context.received ? { received: context.received } : {}
		}
	}),
	[InvalidApiKeyError]: (context) => new AuthError(`The apikey header matched no key configured for auth mode(s): ${quoteList(context.authModes)}.`, InvalidApiKeyError, 401, {
		hint: apiKeyHint(context),
		details: {
			acceptedAuthModes: context.authModes,
			received: context.received,
			...context.configuredKeyNames ? { configuredKeyNames: context.configuredKeyNames } : {}
		}
	}),
	[InvalidJwtError]: (context) => new AuthError(`The JWT in the Authorization header failed verification: ${context.reason}.`, InvalidJwtError, 401, {
		hint: context.hint,
		details: {
			...context.authModes ? { acceptedAuthModes: context.authModes } : {},
			...context.received ? { received: context.received } : {},
			...context.jwt ? { jwt: context.jwt } : {}
		},
		cause: context.cause
	}),
	[JwksNotConfiguredError]: (context = {}) => new AuthError("A JWT was provided but no JWKS is configured, so it cannot be verified. This is a server configuration problem, not a problem with the request.", JwksNotConfiguredError, 500, {
		hint: "Set SUPABASE_JWKS_URL (e.g. https://<project-ref>.supabase.co/auth/v1/.well-known/jwks.json) or SUPABASE_JWKS (inline JSON), or pass " + (context.middleware ? `\`jwks\` to ${context.middleware}()` : "`env.jwks`") + ". Note that a malformed value resolves to null rather than erroring: SUPABASE_JWKS must be valid JSON, and SUPABASE_JWKS_URL must be https (plain http is only allowed for localhost).",
		details: {
			...context.authModes ? { acceptedAuthModes: context.authModes } : {},
			...context.received ? { received: context.received } : {},
			...context.middleware ? { middleware: context.middleware } : {}
		}
	}),
	[JwksFetchFailedError]: (context) => new AuthError(`The remote JWKS could not be fetched, so the JWT could not be verified: ${context.reason}. This is a server-side or upstream failure, not a problem with the request.`, JwksFetchFailedError, 500, {
		hint: "Check that SUPABASE_JWKS_URL points at a reachable JWKS endpoint and that the server has outbound network access to it. The endpoint must return 200 with a JSON `{ \"keys\": [...] }` body.",
		details: {
			...context.authModes ? { acceptedAuthModes: context.authModes } : {},
			...context.received ? { received: context.received } : {}
		},
		cause: context.cause
	}),
	[NoKeysConfiguredError]: (context) => {
		const envVar = context.keyKind === "publishable" ? "SUPABASE_PUBLISHABLE_KEY / SUPABASE_PUBLISHABLE_KEYS" : "SUPABASE_SECRET_KEY / SUPABASE_SECRET_KEYS";
		const configured = context.configuredKeyNames?.[context.keyKind] ?? [];
		return new AuthError(`Auth mode "${context.mode}" was requested but no matching ${context.keyKind} key is configured, so the mode could never match any request. This is a server configuration problem, not a problem with the request.`, NoKeysConfiguredError, 500, {
			hint: `${configuredNames(configured)} Set ${envVar} (the plural form is a JSON object of name → key), or pass \`env.${context.keyKind}Keys\`.`,
			details: {
				acceptedAuthModes: context.authModes,
				received: context.received,
				mode: context.mode,
				keyKind: context.keyKind,
				...context.configuredKeyNames ? { configuredKeyNames: context.configuredKeyNames } : {}
			}
		});
	},
	[InvalidCredentialsError]: (context) => new AuthError(context ? `No credential matched any of the accepted auth mode(s): ${quoteList(context.authModes)}.` : "No credential matched any accepted auth mode.", InvalidCredentialsError, 401, {
		hint: context ? sendOneOfHint(context.authModes) : "Check that the request carries a credential for one of the accepted auth modes.",
		details: context ? {
			acceptedAuthModes: context.authModes,
			received: context.received
		} : void 0
	}),
	[CreateSupabaseClientError]: (options) => new AuthError("Authentication succeeded but the Supabase client could not be created.", CreateSupabaseClientError, 500, {
		hint: "This is almost always a missing or malformed SUPABASE_URL or API key. The underlying error is attached as `cause` — log it to see which value is at fault.",
		cause: options?.cause
	}),
	[UnsupportedRoleError]: (context) => {
		const { requestedRole, supportedRoles } = context;
		const supported = quoteList(supportedRoles);
		const [reason, hint] = requestedRole === "service_role" ? ["the caller's token carries the \"service_role\" role, which withPostgresClient will not assume", "That role bypasses RLS — the guarantee this middleware exists to provide. If bypassing RLS is intended, compose withPostgresAdminClient from '@supabase/server/middleware/postgres-admin'."] : typeof requestedRole === "string" ? [`the caller's token carries the role "${requestedRole}", which withPostgresClient does not support yet`, `It assumes ${supported} only. Custom roles are on the roadmap; until then, issue tokens with one of the supported roles.`] : [`the caller's token has a "role" claim that is not a string (${JSON.stringify(requestedRole)})`, `withPostgresClient assumes ${supported} only and will not guess what a malformed claim meant. Check how the token is minted.`];
		return new AuthError(`Cannot select a Postgres role: ${reason}.`, UnsupportedRoleError, 500, {
			hint,
			details: {
				requestedRole,
				supportedRoles
			}
		});
	}
};
/**
* Factory map for all error types. Keyed by error code constant, each entry
* returns a pre-configured {@link EnvError} or {@link AuthError} complete with
* `hint`, `docs`, and `details`.
*
* @example Throwing typed errors
* ```ts
* throw Errors[MissingSupabaseURLError]()
* throw Errors[MissingPublishableKeyError]('mobile', ['default', 'web'])
* ```
*
* @category Errors
*/
const Errors = {
	...EnvErrorMap,
	...AuthErrorMap
};
//#endregion
Object.defineProperty(exports, "AuthError", {
	enumerable: true,
	get: function() {
		return AuthError;
	}
});
Object.defineProperty(exports, "AuthGenericError", {
	enumerable: true,
	get: function() {
		return AuthGenericError;
	}
});
Object.defineProperty(exports, "CreateSupabaseClientError", {
	enumerable: true,
	get: function() {
		return CreateSupabaseClientError;
	}
});
Object.defineProperty(exports, "EnvError", {
	enumerable: true,
	get: function() {
		return EnvError;
	}
});
Object.defineProperty(exports, "EnvGenericError", {
	enumerable: true,
	get: function() {
		return EnvGenericError;
	}
});
Object.defineProperty(exports, "ErrorCodeHeader", {
	enumerable: true,
	get: function() {
		return ErrorCodeHeader;
	}
});
Object.defineProperty(exports, "ErrorSource", {
	enumerable: true,
	get: function() {
		return ErrorSource;
	}
});
Object.defineProperty(exports, "Errors", {
	enumerable: true,
	get: function() {
		return Errors;
	}
});
Object.defineProperty(exports, "InvalidApiKeyError", {
	enumerable: true,
	get: function() {
		return InvalidApiKeyError;
	}
});
Object.defineProperty(exports, "InvalidCredentialsError", {
	enumerable: true,
	get: function() {
		return InvalidCredentialsError;
	}
});
Object.defineProperty(exports, "InvalidJwtError", {
	enumerable: true,
	get: function() {
		return InvalidJwtError;
	}
});
Object.defineProperty(exports, "JwksFetchFailedError", {
	enumerable: true,
	get: function() {
		return JwksFetchFailedError;
	}
});
Object.defineProperty(exports, "JwksNotConfiguredError", {
	enumerable: true,
	get: function() {
		return JwksNotConfiguredError;
	}
});
Object.defineProperty(exports, "MissingAuthorizationServerError", {
	enumerable: true,
	get: function() {
		return MissingAuthorizationServerError;
	}
});
Object.defineProperty(exports, "MissingConnectionStringError", {
	enumerable: true,
	get: function() {
		return MissingConnectionStringError;
	}
});
Object.defineProperty(exports, "MissingCredentialsError", {
	enumerable: true,
	get: function() {
		return MissingCredentialsError;
	}
});
Object.defineProperty(exports, "MissingDefaultPublishableKeyError", {
	enumerable: true,
	get: function() {
		return MissingDefaultPublishableKeyError;
	}
});
Object.defineProperty(exports, "MissingDefaultSecretKeyError", {
	enumerable: true,
	get: function() {
		return MissingDefaultSecretKeyError;
	}
});
Object.defineProperty(exports, "MissingPublishableKeyError", {
	enumerable: true,
	get: function() {
		return MissingPublishableKeyError;
	}
});
Object.defineProperty(exports, "MissingResourceServerError", {
	enumerable: true,
	get: function() {
		return MissingResourceServerError;
	}
});
Object.defineProperty(exports, "MissingSecretKeyError", {
	enumerable: true,
	get: function() {
		return MissingSecretKeyError;
	}
});
Object.defineProperty(exports, "MissingSupabaseURLError", {
	enumerable: true,
	get: function() {
		return MissingSupabaseURLError;
	}
});
Object.defineProperty(exports, "NoKeysConfiguredError", {
	enumerable: true,
	get: function() {
		return NoKeysConfiguredError;
	}
});
Object.defineProperty(exports, "SupabaseServerError", {
	enumerable: true,
	get: function() {
		return SupabaseServerError;
	}
});
Object.defineProperty(exports, "UnsupportedRoleError", {
	enumerable: true,
	get: function() {
		return UnsupportedRoleError;
	}
});
Object.defineProperty(exports, "UnusableCredentialError", {
	enumerable: true,
	get: function() {
		return UnusableCredentialError;
	}
});
