Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_errors = require("./errors-ClZWGicV.cjs");
const require_verify_auth = require("./verify-auth-_AbEWG_L.cjs");
const require_error_response = require("./error-response-CRndotyA.cjs");
const require_admin_client = require("./admin-client-D6sixvZx.cjs");
const require_middleware_client_index = require("./middleware/client/index.cjs");
const require_create_supabase_context = require("./create-supabase-context-i3Eg5Sqk.cjs");
const require_with_oauth_protected_resource = require("./with-oauth-protected-resource-cZ4wsT1x.cjs");
let _supabase_supabase_js_cors = require("@supabase/supabase-js/cors");
let _supabase_middleware = require("@supabase/middleware");
//#region src/cors.ts
/**
* Whether the given CORS configuration disables CORS handling.
*
* @param config - The CORS configuration.
* @returns `true` for `'disabled'` or the deprecated `false`, otherwise `false`.
*
* @internal
*/
function isCorsDisabled(config) {
	return config === false || config === "disabled";
}
/**
* Builds the CORS headers object based on the given configuration.
*
* @param config - The CORS configuration.
* @returns A headers record to include in the response. Empty object if CORS is disabled.
*
* @internal
*/
function buildCorsHeaders(config) {
	if (isCorsDisabled(config)) return {};
	if (typeof config === "object") {
		if ("headers" in config && typeof config.headers === "object") return config.headers;
		return config;
	}
	return _supabase_supabase_js_cors.corsHeaders;
}
/**
* Returns a new `Response` with CORS headers appended.
*
* Creates a clone of the original response and sets each CORS header on it.
* If CORS is disabled (`'disabled'` or the deprecated `false`), returns the original response unchanged.
*
* @param response - The original response to augment.
* @param config - The CORS configuration.
* @returns A new `Response` with CORS headers set, or the original response if CORS is disabled.
*
* @internal
*/
function addCorsHeaders(response, config) {
	if (isCorsDisabled(config)) return response;
	const corsHeaders = buildCorsHeaders(config);
	const newResponse = new Response(response.body, response);
	for (const [key, value] of Object.entries(corsHeaders)) newResponse.headers.set(key, value);
	return newResponse;
}
//#endregion
//#region src/with-supabase.ts
function withSupabase(config, handler) {
	const clientEntries = [require_middleware_client_index.withSupabaseClient({
		env: config.env,
		supabaseOptions: config.supabaseOptions
	}), require_admin_client.withSupabaseAdminClient({
		env: config.env,
		supabaseOptions: config.supabaseOptions
	})];
	const userComposed = (config.middleware ?? []).reduceRight((h, entry) => entry(h), handler);
	return async (req, platformArg) => {
		const errorHeaders = () => {
			if (isCorsDisabled(config.cors)) return {};
			const headers = buildCorsHeaders(config.cors);
			const exposeKey = Object.keys(headers).find((name) => name.toLowerCase() === "access-control-expose-headers") ?? "Access-Control-Expose-Headers";
			const exposed = headers[exposeKey];
			return {
				...headers,
				[exposeKey]: exposed ? `${exposed}, ${require_errors.ErrorCodeHeader}` : require_errors.ErrorCodeHeader
			};
		};
		if (!isCorsDisabled(config.cors) && req.method === "OPTIONS") return new Response(null, {
			status: 204,
			headers: buildCorsHeaders(config.cors)
		});
		const { data: auth, error } = await require_verify_auth.verifyAuth(req, {
			auth: config.auth,
			allow: config.allow,
			env: config.env
		});
		if (error) return require_error_response.errorResponse(error, {
			headers: errorHeaders(),
			errors: config.errors
		});
		let inClientPhase = true;
		const markUserPhase = (r, ctx) => {
			inClientPhase = false;
			return userComposed(r, ctx);
		};
		const composed = clientEntries.reduceRight((h, entry) => entry(h), markUserPhase);
		let response;
		try {
			const baseContext = (0, _supabase_middleware.isContext)(platformArg) ? platformArg : (0, _supabase_middleware.seedContext)(platformArg);
			const upstreamAuth = {
				authMode: auth.authMode,
				authKeyName: auth.keyName ?? void 0
			};
			response = await composed(req, {
				...baseContext,
				userClaims: auth.userClaims,
				jwtClaims: auth.jwtClaims,
				...upstreamAuth
			});
		} catch (e) {
			const mapped = !inClientPhase ? null : e instanceof require_errors.EnvError ? new require_errors.AuthError(e.message, e.code, 500, {
				hint: e.hint,
				details: e.details,
				docs: e.docs,
				cause: e
			}) : e instanceof require_errors.AuthError && e.code === "CREATE_SUPABASE_CLIENT_ERROR" ? e : null;
			if (!mapped) throw e;
			return require_error_response.errorResponse(mapped, {
				headers: errorHeaders(),
				errors: config.errors
			});
		}
		if (!isCorsDisabled(config.cors)) return addCorsHeaders(response, config.cors);
		return response;
	};
}
//#endregion
exports.AuthError = require_errors.AuthError;
exports.AuthGenericError = require_errors.AuthGenericError;
exports.CreateSupabaseClientError = require_errors.CreateSupabaseClientError;
exports.EnvError = require_errors.EnvError;
exports.EnvGenericError = require_errors.EnvGenericError;
exports.ErrorCodeHeader = require_errors.ErrorCodeHeader;
exports.ErrorSource = require_errors.ErrorSource;
exports.Errors = require_errors.Errors;
exports.InvalidApiKeyError = require_errors.InvalidApiKeyError;
exports.InvalidCredentialsError = require_errors.InvalidCredentialsError;
exports.InvalidJwtError = require_errors.InvalidJwtError;
exports.JwksFetchFailedError = require_errors.JwksFetchFailedError;
exports.JwksNotConfiguredError = require_errors.JwksNotConfiguredError;
exports.MissingAuthorizationServerError = require_errors.MissingAuthorizationServerError;
exports.MissingConnectionStringError = require_errors.MissingConnectionStringError;
exports.MissingCredentialsError = require_errors.MissingCredentialsError;
exports.MissingDefaultPublishableKeyError = require_errors.MissingDefaultPublishableKeyError;
exports.MissingDefaultSecretKeyError = require_errors.MissingDefaultSecretKeyError;
exports.MissingPublishableKeyError = require_errors.MissingPublishableKeyError;
exports.MissingResourceServerError = require_errors.MissingResourceServerError;
exports.MissingSecretKeyError = require_errors.MissingSecretKeyError;
exports.MissingSupabaseURLError = require_errors.MissingSupabaseURLError;
exports.NoKeysConfiguredError = require_errors.NoKeysConfiguredError;
exports.SupabaseServerError = require_errors.SupabaseServerError;
exports.UnsupportedRoleError = require_errors.UnsupportedRoleError;
exports.UnusableCredentialError = require_errors.UnusableCredentialError;
exports.createSupabaseContext = require_create_supabase_context.createSupabaseContext;
exports.fromSupabaseUrl = require_with_oauth_protected_resource.fromSupabaseUrl;
exports.resourceMetadataResponse = require_with_oauth_protected_resource.resourceMetadataResponse;
exports.unauthorizedResponse = require_with_oauth_protected_resource.unauthorizedResponse;
exports.withOAuthProtectedResource = require_with_oauth_protected_resource.withOAuthProtectedResource;
exports.withSupabase = withSupabase;
