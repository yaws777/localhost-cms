const require_errors = require("./errors-ClZWGicV.cjs");
let _supabase_middleware = require("@supabase/middleware");
//#region src/oauth-protected-resource/paths.ts
/**
* Supabase platform URL path prefixes used when reconstructing a resource's
* external URLs. Kept in one place so the magic strings have a single source of
* truth across the URL derivation and the middleware.
*
* @module
*/
/**
* Path prefix the Supabase Edge Functions platform proxy strips from the request
* before invoking the function, and which the Edge Functions default restores
* when reconstructing the resource's external path.
*
* @internal
*/
const EDGE_FUNCTIONS_PATH_PREFIX = "/functions/v1";
/**
* Path prefix of the Supabase Auth API, appended to the project's base URL to
* form the OAuth authorization server URL advertised in the resource metadata.
*
* @internal
*/
const AUTH_PATH_PREFIX = "/auth/v1";
//#endregion
//#region src/oauth-protected-resource/runtime.ts
/**
* Whether this request is being served by Supabase Edge Functions.
*
* Gates the request-derived URL defaults. The Edge Functions gateway sets
* `X-Forwarded-*` to the project's externally-visible origin and strips the
* `/functions/v1` prefix; off platform those headers describe the app's own
* origin, which is unrelated to the Supabase project.
*
* True when `SUPABASE_FUNCTION_SLUG` is set, or when the host runtime is Deno.
* A plain Deno server or Deno Deploy therefore reads as Edge Functions.
*
* @internal
*/
function isEdgeFunctions() {
	if ((0, _supabase_middleware.getEnv)("SUPABASE_FUNCTION_SLUG")) return true;
	return _supabase_middleware.runtimeName === "deno";
}
//#endregion
//#region src/oauth-protected-resource/url.ts
/**
* Trailing path segment where a resource serves its OAuth Protected Resource
* Metadata document (RFC 9728), as a stripping pattern.
*
* @internal
*/
const METADATA_SUFFIX_PATTERN = /\/oauth-protected-resource$/;
/** Strips a trailing slash so URL concatenation doesn't produce `//`. @internal */
function trimTrailingSlash(value) {
	return value.endsWith("/") ? value.slice(0, -1) : value;
}
/**
* Percent-encodes `"` and `\` — invalid URL code points that would otherwise
* break out of the RFC 9110 quoted-string in `WWW-Authenticate`. Applied to
* every advertised URL, so the header, the metadata document, and the ctx
* contribution agree on one spelling (RFC 9728 §3.3 requires an exact match).
*
* @internal
*/
function percentEncodeQuotes(value) {
	return value.replace(/["\\]/g, (c) => c === "\"" ? "%22" : "%5C");
}
/**
* The externally-visible origin of a Supabase Edge Function.
*
* `SUPABASE_PUBLIC_URL` wins when set. Otherwise the origin is assembled from
* `X-Forwarded-Host`, `-Proto` and `-Port`, each falling back to the request
* URL:
*
* - The port comes from `X-Forwarded-Port` only when the host does not already
*   carry one.
* - A port that is standard for the scheme (443 on https, 80 on http) is
*   omitted.
* - The scheme is lowercased.
* - Header values are used as-is, never comma-split.
*
* `SUPABASE_URL` is not consulted — see {@link defaultAuthorizationServer}.
*
* @internal
*/
function edgeOrigin(req) {
	const publicUrl = (0, _supabase_middleware.getEnv)("SUPABASE_PUBLIC_URL");
	if (publicUrl) return trimTrailingSlash(publicUrl);
	const url = new URL(req.url);
	const host = req.headers.get("X-Forwarded-Host") ?? url.hostname;
	const proto = (req.headers.get("X-Forwarded-Proto") ?? url.protocol.replace(":", "")).toLowerCase();
	const port = host.includes(":") ? "" : req.headers.get("X-Forwarded-Port") ?? url.port;
	return `${proto}://${host}${port && !(proto === "https" && port === "443" || proto === "http" && port === "80") ? `:${port}` : ""}`;
}
/**
* The external path of a Supabase Edge Function.
*
* With `SUPABASE_FUNCTION_SLUG` set, `/functions/v1/{slug}` — canonical, and
* independent of the path the request arrived on. Without it, the received path
* minus the metadata suffix, with the `/functions/v1` prefix restored (every
* gateway fronting Edge Functions strips that prefix).
*
* The two forms differ only for a request at a sub-path of the function: the
* canonical one reports the function, the reconstructed one the sub-path.
*
* @throws {EnvError} `MISSING_RESOURCE_SERVER` on a root path with no slug —
* there is no function segment to restore, and a bare `/functions/v1`
* identifies no resource.
*
* @internal
*/
function edgeResourcePath(req) {
	const slug = (0, _supabase_middleware.getEnv)("SUPABASE_FUNCTION_SLUG");
	if (slug) return `${EDGE_FUNCTIONS_PATH_PREFIX}/${slug}`;
	const received = new URL(req.url).pathname.replace(METADATA_SUFFIX_PATTERN, "");
	if (received === "" || received === "/") throw require_errors.Errors[require_errors.MissingResourceServerError]();
	return `${EDGE_FUNCTIONS_PATH_PREFIX}${received}`;
}
/**
* The resource identifier to advertise when none was configured: on Edge
* Functions, the external origin plus the external path.
*
* There is no environment fallback — `SUPABASE_URL` names the Supabase project,
* not this endpoint — so off Edge Functions it throws.
*
* @throws {EnvError} `MISSING_RESOURCE_SERVER` off Edge Functions, or on a
* root path with no `SUPABASE_FUNCTION_SLUG` — see {@link edgeResourcePath}.
*
* @internal
*/
function defaultResourceServer(req) {
	if (!isEdgeFunctions()) throw require_errors.Errors[require_errors.MissingResourceServerError]();
	return `${edgeOrigin(req)}${edgeResourcePath(req)}`;
}
/**
* The Supabase Auth issuer to advertise when none was configured.
*
* On Edge Functions, the request's external origin plus the Auth path. Off Edge
* Functions the app's origin is unrelated to the project's, so the environment
* answers instead: `SUPABASE_PUBLIC_URL`, then `SUPABASE_URL`, each with the
* Auth path appended.
*
* Both environment rungs sit below the Edge derivation, so on Edge Functions
* neither can displace the origin the client used.
*
* @throws {EnvError} `MISSING_AUTHORIZATION_SERVER` off Edge Functions with
* neither variable set.
*
* @internal
*/
function defaultAuthorizationServer(req) {
	if (isEdgeFunctions()) return `${edgeOrigin(req)}${AUTH_PATH_PREFIX}`;
	const publicUrl = (0, _supabase_middleware.getEnv)("SUPABASE_PUBLIC_URL");
	if (publicUrl) return fromSupabaseUrl(publicUrl);
	const supabaseUrl = (0, _supabase_middleware.getEnv)("SUPABASE_URL");
	if (supabaseUrl) return fromSupabaseUrl(supabaseUrl);
	throw require_errors.Errors[require_errors.MissingAuthorizationServerError]();
}
/**
* **Alpha.** Points `authorizationServer` at a Supabase project's Auth
* issuer.
*
* Use this off Supabase Edge Functions, where the app's own origin is unrelated
* to the Supabase project's, so the issuer cannot be derived from the request.
*
* @param supabaseUrl - The project URL, e.g. `https://<ref>.supabase.co` (the
* same value passed to `createClient()`).
*
* The OAuth Protected Resource surface is alpha — the config shape, the
* contributed context key, and the metadata route may change in a minor
* release.
*
* @alpha
* @category Middleware
*
* @example
* ```ts
* import { withOAuthProtectedResource, fromSupabaseUrl } from '@supabase/server'
*
* withOAuthProtectedResource(
*   {
*     resourceServer: (req) => new URL(req.url).origin + '/api/mcp',
*     authorizationServer: fromSupabaseUrl('https://abc123.supabase.co'),
*   },
*   handler,
* )
* ```
*/
function fromSupabaseUrl(supabaseUrl) {
	const base = trimTrailingSlash(supabaseUrl);
	return base.endsWith("/auth/v1") ? base : `${base}${AUTH_PATH_PREFIX}`;
}
/** Resolves a {@link UrlOption} against a request. @internal */
function resolveUrlOption(option, req, fallback) {
	return percentEncodeQuotes(trimTrailingSlash((typeof option === "function" ? option(req) : option) ?? fallback(req)));
}
/**
* Constructs the external-facing URL of the protected resource, falling back to
* {@link defaultResourceServer} when `resourceServer` is unset.
*
* @internal
*/
function getResourceUrl(req, resourceServer) {
	return resolveUrlOption(resourceServer, req, defaultResourceServer);
}
/**
* Constructs the external-facing URL for the OAuth Protected Resource Metadata
* endpoint (RFC 9728).
*
* @internal
*/
function getResourceMetadataUrl(req, resourceServer) {
	return `${getResourceUrl(req, resourceServer)}/oauth-protected-resource`;
}
/**
* Constructs the authorization server (Supabase Auth issuer) to advertise.
*
* @internal
*/
function getAuthUrl(req, authorizationServer) {
	return resolveUrlOption(authorizationServer, req, defaultAuthorizationServer);
}
//#endregion
//#region src/oauth-protected-resource/responses.ts
/**
* **Alpha.** `401` response with a
* `WWW-Authenticate: Bearer resource_metadata="..."` header (RFC 9728).
* The metadata URL defaults to the Edge Functions derivation and throws off
* platform; pass `resourceMetadataUrl` to override for custom setups.
*
* The OAuth Protected Resource surface is alpha — the config shape, the
* contributed context key, and the metadata route may change in a minor
* release.
*
* @alpha
* @category Middleware
*/
function unauthorizedResponse(req, options) {
	const metadataUrl = percentEncodeQuotes(options?.resourceMetadataUrl ?? getResourceMetadataUrl(req));
	return new Response(JSON.stringify({ error: "Unauthorized" }), {
		status: 401,
		headers: {
			"Content-Type": "application/json",
			"WWW-Authenticate": `Bearer resource_metadata="${metadataUrl}"`
		}
	});
}
/**
* **Alpha.** RFC 9728 OAuth Protected Resource Metadata response.
* Advertises the authorization server, resource URI, and bearer methods supported.
* URLs default to the Edge Functions derivation and throw off platform; pass
* `resource` / `authorizationServers` to override.
*
*
* The OAuth Protected Resource surface is alpha — the config shape, the
* contributed context key, and the metadata route may change in a minor
* release.
*
* @alpha
* @category Middleware
*/
function resourceMetadataResponse(req, options) {
	const resource = options?.resource ?? getResourceUrl(req);
	const authorizationServers = options?.authorizationServers ?? [getAuthUrl(req)];
	return new Response(JSON.stringify({
		resource,
		authorization_servers: authorizationServers,
		bearer_methods_supported: ["header"]
	}), {
		status: 200,
		headers: {
			"Content-Type": "application/json",
			"Access-Control-Allow-Origin": "*"
		}
	});
}
//#endregion
//#region src/oauth-protected-resource/with-oauth-protected-resource.ts
/**
* **Alpha.** Wraps a request handler with OAuth 2.1 Protected Resource
* behavior (RFC 9728).
*
* - Serves OAuth Protected Resource Metadata at `GET {resource}/oauth-protected-resource`
*   (with permissive CORS, including the `OPTIONS` preflight, so browser-based clients can read it)
* - Enriches a `401` from the inner handler with `WWW-Authenticate: Bearer resource_metadata="..."`,
*   unless the handler already set a `WWW-Authenticate` header (its value wins)
* - Passes any other path through to the inner handler unchanged (composition,
*   not routing, decides what happens to it)
*
* The metadata route is matched on the path *suffix*, so **any** `GET` or
* `OPTIONS` ending in `/oauth-protected-resource` is answered here and never
* reaches the inner handler, at any depth. Other methods pass through.
*
* Zero-config on Supabase Edge Functions. Elsewhere
* {@link OAuthProtectedResourceConfig.resourceServer} is required and
* {@link OAuthProtectedResourceConfig.authorizationServer} falls back to
* `SUPABASE_URL`; each throws an `EnvError` when it cannot be resolved.
*
* Contributes `ctx.oauthProtectedResource` (the resolved metadata URL) to the
* downstream context. Nested under `withSupabase`, the key is typed on the
* handler's `ctx` when the outermost call is anchored with
* `satisfies FetchHandler` — see `withSupabase`'s type note.
*
* The OAuth Protected Resource surface is alpha — the config shape, the
* contributed context key, and the metadata route may change in a minor
* release.
*
* @alpha
* @category Middleware
*
* @example Supabase Edge Functions — zero config
* ```ts
* import { withOAuthProtectedResource, withSupabase } from '@supabase/server'
*
* Deno.serve(
*   withOAuthProtectedResource(
*     withSupabase({ auth: 'user' }, async (_req, { supabase }) => {
*       const { data, error } = await supabase.from('items').select('*')
*       if (error) throw error
*       return Response.json(data)
*     }),
*   ),
* )
* ```
*
* @example Any other backend
* ```ts
* import { withOAuthProtectedResource, fromSupabaseUrl } from '@supabase/server'
*
* export default {
*   fetch: withOAuthProtectedResource(
*     {
*       resourceServer: (req) => new URL(req.url).origin + '/api/mcp',
*       authorizationServer: fromSupabaseUrl('https://abc123.supabase.co'),
*     },
*     handler,
*   ),
* }
* ```
*
* @example A non-Supabase authorization server
* ```ts
* withOAuthProtectedResource(
*   {
*     resourceServer: 'https://api.example.com/mcp',
*     authorizationServer: 'https://example.clerk.accounts.dev',
*   },
*   handler,
* )
* ```
*/
const withOAuthProtectedResource = (0, _supabase_middleware.defineMiddleware)({
	key: "oauthProtectedResource",
	run: (config) => async function* (req) {
		const isMetadataRoute = new URL(req.url).pathname.endsWith("/oauth-protected-resource");
		if (isMetadataRoute && req.method === "GET") return resourceMetadataResponse(req, {
			resource: getResourceUrl(req, config?.resourceServer),
			authorizationServers: [getAuthUrl(req, config?.authorizationServer)]
		});
		if (isMetadataRoute && req.method === "OPTIONS") return new Response(null, {
			status: 204,
			headers: {
				"Access-Control-Allow-Origin": "*",
				"Access-Control-Allow-Methods": "GET, OPTIONS",
				"Access-Control-Allow-Headers": "content-type, mcp-protocol-version"
			}
		});
		const resourceMetadataUrl = getResourceMetadataUrl(req, config?.resourceServer);
		const response = yield { oauthProtectedResource: { resourceMetadataUrl } };
		if (response.status === 401 && !response.headers.has("WWW-Authenticate")) {
			const challenge = `Bearer resource_metadata="${resourceMetadataUrl}"`;
			try {
				response.headers.set("WWW-Authenticate", challenge);
				return response;
			} catch {
				const headers = new Headers(response.headers);
				headers.set("WWW-Authenticate", challenge);
				return new Response(response.bodyUsed ? null : response.body, {
					status: response.status,
					statusText: response.statusText,
					headers
				});
			}
		}
		return response;
	}
});
//#endregion
Object.defineProperty(exports, "fromSupabaseUrl", {
	enumerable: true,
	get: function() {
		return fromSupabaseUrl;
	}
});
Object.defineProperty(exports, "resourceMetadataResponse", {
	enumerable: true,
	get: function() {
		return resourceMetadataResponse;
	}
});
Object.defineProperty(exports, "unauthorizedResponse", {
	enumerable: true,
	get: function() {
		return unauthorizedResponse;
	}
});
Object.defineProperty(exports, "withOAuthProtectedResource", {
	enumerable: true,
	get: function() {
		return withOAuthProtectedResource;
	}
});
